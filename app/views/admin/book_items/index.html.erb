<h4>Все экземпляры выбранной книги:</h4>
<p></p>

<h5>Название:"<%= @book.name   %>" </h5>
<h5>Автор:<%= @book.author %> </h4>


<div class="table-responsive">
<table class="table table-striped">
  
  <tr>
      <th>N n/n</th>
      <th>Кто добавил:</th>
      <th>Время создания:</th>
      <th>Текущий владелец:</th>
      <th>Когда утилизируется: </th>
      
  </tr>
  
    <% @book_items.each do |item| %>
      <tr>
        <td><%= item.id %></td>
        <td><%= item.user.first_name %> <%= item.user.last_name %></td>
        <td><%= item.created_at %></td>
        <td><%= item.user.first_name %></td>
        <td><%= item.archived_at %></td>
       
      </tr>
    <% end %>
  

</table>

</div>

<%= link_to "Добавить новый экземпляр", new_admin_book_book_item_path %>
