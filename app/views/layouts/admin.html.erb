<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css?family=Lobster&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Courgette|Lobster&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Courgette|Lobster|Spectral&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag  "application", "data-turbolinks-track" => true %>
    <title>Library</title>
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">Абонемент
          <img src="/assets/books16.png" width="64" height="64" alt="">
        </a>
        <div class="collapse navbar-collapse" id="navbarNav">
          <% if user_signed_in? %>
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="<%=  current_user.email%>"></a>  <!-- <span class="sr-only">(current)</span> -->
              </li>
              <li class="nav-item">
                <%= link_to 'Выход', destroy_user_session_path, :method => 'delete', class: 'btn btn-outline-primary pull-xs-right' %>
              </li>
            </ul>
          <% else %>
            <ul class="navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" href="<%= new_user_session_path%>">ВХОД <!-- <span class="sr-only">(current)</span> --></a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="<%= new_user_registration_path%>">РЕГИСТРАЦИЯ</a>
              </li>
            </ul>
          <% end %>
        </div>
      </nav>
    </header>

<div class="card">
  <div class="card-header">
    <h3 class="card-title">Информация только для администраторов </h3>
    <a class="btn btn-outline-primary" href=<%=admin_users_path%> role="button">Список уже зарегистрированных</a>
    <a class="btn btn-outline-primary" href=<%=list_wait_admin_users_path%> role="button">Список, ожидающих регистрацию</a>
    <a class="btn btn-outline-primary" href=<%=admin_fines_path%> role="button">Список штрафников</a>
    <a class="btn btn-outline-primary" href=<%=admin_books_path%> role="button">Учет и пополнение книг</a>
    <a class="btn btn-outline-primary" href=<%=admin_book_types_path%> role="button">Категории книг</a>
    <a class="btn btn-outline-primary" href=<%=admin_info_index_path%> role="button">Все администраторы</a>
    <a class="btn btn-outline-primary" href=<%=admin_book_requests_path%> role="button">Журнал событий</a>
  </div>

  <% if @errors %>
  <%= render 'layouts/error_messages' %>
  <% end %>

    <%= yield %>
</div>



</body>
</html>